# 任务监控

入口：目标任务详情侧栏 → 监控。来源：用户教学。

选择目标实例（单实例通常 `worker-0`），读取 CPU、主存、GPU 利用率。用时间范围下拉框和底部滑块定位所需时段。

**完成判据**：给出实例、时间范围和相关资源曲线表现。申请核数/容量不是利用率；资源曲线不能单独证明训练完成或定位具体代码耗时。

**跳转**：实例“查看日志” → [日志](logs.md)；详情侧栏可切换[摘要](summary.md)、[日志](logs.md)、[终端](terminal.md)。

**辅助**：需要把利用率变化对应到训练阶段 → [日志](logs.md)。
