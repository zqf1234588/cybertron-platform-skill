# SFTP 服务摘要

归属：工作空间 → 工具服务 → 目标 SFTP → 摘要。来源：用户教学。

基本信息：名称、项目、类型、状态、服务地址、用户名、遮蔽密码、镜像、CPU/主存、空闲超时、创建者、创建/开始/结束时间、运行时长。实例表显示实例状态、资源与时间。

CPU 和主存按目标服务的实际配置核对。连接地址必须读取当前服务的主机和端口，不复用截图；不为查看摘要主动显示密码，不保存凭据。

**完成判据**：目标服务所需状态或连接信息已核对。

**跳转**：服务详情侧栏“日志” → 服务日志页（操作留空）。

## 启停、连接与日志操作

留空。
