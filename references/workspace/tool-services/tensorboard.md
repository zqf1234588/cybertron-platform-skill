# TensorBoard 服务摘要

归属：工作空间 → 工具服务 → 目标 TensorBoard → 摘要。来源：用户教学。

核对项目、状态、**服务地址、日志目录**、镜像、CPU/主存、创建/开始/结束时间、运行时长及实例状态。资源值以当前服务配置为准。

日志目录决定该服务读取哪些事件；截断字段不能当作完整路径。此处“日志”是服务进程输出，不是训练 worker 的输出，也不是训练指标事件文件。

**完成判据**：服务身份、读取目录和所需状态/地址已核实。

**跳转**：服务地址 → [TensorBoard 应用](../../apps/tensorboard.md)；侧栏“日志”或实例“查看日志” → 服务日志页（内部操作留空）。

## 启停与服务日志操作

留空。
